import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Remove 'output: export' to support API routes
  // For static deployment, use a serverless platform like Vercel
};

export default nextConfig;
